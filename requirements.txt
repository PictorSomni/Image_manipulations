# Dashboard Image Manipulation - Dependencies
# Compatible: Windows, macOS, Linux

# Interface graphique
flet>=0.84.0
flet-desktop>=0.84.0

# Traitement d'images
Pillow>=12.1.0,<13.0.0
Wand>=0.6.11
numpy>=2.0.0

# Conversion PDF en images
PyMuPDF>=1.23.0

# Suppression de fond par IA (requis par Recadrage manuel.pyw)
rembg>=2.0.75
# Backend ONNX GPU (fallback CPU automatique si aucun GPU compatible detecte)
onnxruntime-gpu>=1.16.0

# Amélioration par IA (requis par Augmentation IA.py)
spandrel>=0.3.0
torch>=2.5.1

# Segmentation interactive SAM2 (requis par Augmentation IA.py)
# Installation :
#   pip install git+https://github.com/facebookresearch/sam2.git
# sam2  # pas disponible sur PyPI

# Inpainting IA (requis par Augmentation IA.py)
iopaint>=1.6.0
# Après installation, télécharger les modèles :
#   iopaint download --model lama   (~100 MB, déjà inclus)
#   iopaint download --model mat    (~400 MB, meilleur pour objets/personnes)

# Synthèse par patches sans IA (requis par Augmentation IA.py)
# Remplace opencv-python si déjà installé :
#   pip uninstall opencv-python && pip install opencv-contrib-python
opencv-contrib-python>=4.8.0

# Lecture de fichiers Word (requis par l'assistant IA — documents .docx joints)
python-docx>=1.1.0

# API Google Gemini (requis par l'assistant IA — modèles Gemini)
google-genai>=1.55.0

# Lecture audio TTS (requis par la synthèse vocale Gemini)
# Windows/macOS : fonctionne nativement.
# Linux : apt install libportaudio2  (ou dnf install portaudio)
sounddevice>=0.5.0

# Certificats système pour HTTPS (requis par flet, rembg et iopaint)
pip-system-certs

# Windows : accès aux fonctionnalités spécifiques (ex: notifications, clipboard)
pywin32