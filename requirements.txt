# Dashboard Image Manipulation - Dépendances
# Compatible : Windows, macOS, Linux

# ──────────────────────────────────────────────────────────────────────────────
# INTERFACE GRAPHIQUE
# ──────────────────────────────────────────────────────────────────────────────

flet>=0.85.0
flet-desktop>=0.85.0
flet-code-editor>=0.85.0


# ──────────────────────────────────────────────────────────────────────────────
# TRAITEMENT D'IMAGES
# ──────────────────────────────────────────────────────────────────────────────

# Pillow 12.x — version courante, requis par flet, rembg et les scripts internes.
# IMPORTANT : IOPaint requiert Pillow<10.0.0 et n'est pas compatible avec cette
# version. IOPaint est donc optionnel (voir requirements-augmentation.txt).
Pillow>=12.1.0

# ImageMagick Python binding (requis par plusieurs scripts de conversion)
Wand>=0.6.11

numpy>=1.24.0


# ──────────────────────────────────────────────────────────────────────────────
# CONVERSION PDF EN IMAGES
# ──────────────────────────────────────────────────────────────────────────────

PyMuPDF>=1.23.0


# ──────────────────────────────────────────────────────────────────────────────
# SUPPRESSION DE FOND PAR IA
# Requis par : Recadrage manuel.pyw
# ──────────────────────────────────────────────────────────────────────────────

rembg>=2.0.57

# Backend ONNX GPU — fallback CPU automatique si aucun GPU compatible détecté.
# Le script d'installation bascule sur 'onnxruntime' (CPU) si ce paquet échoue.
onnxruntime-gpu>=1.16.0


# ──────────────────────────────────────────────────────────────────────────────
# ASSISTANT IA
# ──────────────────────────────────────────────────────────────────────────────

# API Google Gemini (modèles Gemini 3.x, génération d'images, TTS)
google-genai>=1.55.0

# Lecture de fichiers Word (.docx) joints à l'assistant IA
python-docx>=1.1.0

# Lecture de fichiers tableur joints à l'assistant IA
openpyxl>=3.1.0          # .xlsx / .xlsm
xlrd>=2.0.1              # .xls (ancien format Excel)
odfpy>=1.4.1             # .ods / .fods (LibreOffice / OpenOffice)

# Synthèse vocale TTS — Windows/macOS : fonctionne nativement.
# Linux : sudo apt install libportaudio2  (ou sudo dnf install portaudio)
sounddevice>=0.5.0

# Capture d'écran et contrôle souris/clavier (computer use)
# Linux : sudo apt install python3-xlib scrot  (ou dnf install python3-xlib scrot)
pyautogui>=0.9.54


# ──────────────────────────────────────────────────────────────────────────────
# RÉSEAU & CERTIFICATS
# ──────────────────────────────────────────────────────────────────────────────

# Certificats système pour les requêtes HTTPS (requis par flet, rembg, google-genai)
pip-system-certs


# ──────────────────────────────────────────────────────────────────────────────
# IDENTIFIANTS / MOTS DE PASSE
# ──────────────────────────────────────────────────────────────────────────────

# Coffre à secrets natif de l'OS : Windows Credential Manager, macOS Keychain,
# Secret Service sur Linux (voir Data/credentials.py).
# Linux headless : sudo apt install gnome-keyring  (ou pip install secretstorage)
keyring>=25.0.0

# Client SSH pur Python — permet de fournir le mot de passe en mémoire
# (résolu via credentials.py) sans jamais l'exposer en ligne de commande.
# Utilisé par l'outil IA ssh_command (Data/ai_tools.py).
paramiko>=3.4.0


# ──────────────────────────────────────────────────────────────────────────────
# WINDOWS UNIQUEMENT
# ──────────────────────────────────────────────────────────────────────────────

# Accès aux fonctionnalités système Windows (clipboard, notifications…)
# Ignoré automatiquement sur macOS et Linux.
pywin32; sys_platform == "win32"


# ──────────────────────────────────────────────────────────────────────────────
# OPTIONNEL — AUGMENTATION IA  (Augmentation IA.py)
# ──────────────────────────────────────────────────────────────────────────────
# Ces paquets sont volumineux (~5–10 GB) et en conflit avec Pillow>=12.
# Ils ne sont PAS installés par défaut.
#
# Pour les installer : pip install -r requirements-augmentation.txt
#
# Contenu :
#   iopaint>=1.6.0              — inpainting IA (LaMa, MAT…)
#   spandrel>=0.3.0             — super-résolution
#   torch>=2.5.1                — backend PyTorch
#   opencv-contrib-python>=4.8.0 — synthèse par patches (remplace opencv-python)
#   Pillow==9.5.0               — version épinglée, requise par iopaint
#
# Segmentation interactive SAM2 (non disponible sur PyPI) :
#   pip install git+https://github.com/facebookresearch/sam2.git
#
# Modèles IOPaint à télécharger après installation :
#   iopaint download --model lama   (~100 MB)
#   iopaint download --model mat    (~400 MB)
